import { FilterSucursalPipe } from './filter-sucursal.pipe';

describe('FilterSucursalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSucursalPipe();
    expect(pipe).toBeTruthy();
  });
});
