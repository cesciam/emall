import { FiltroItemPipe } from './filtro-item.pipe';

describe('FiltroItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroItemPipe();
    expect(pipe).toBeTruthy();
  });
});
