import { FilterCompraPipe } from './filter-compra.pipe';

describe('FilterCompraPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompraPipe();
    expect(pipe).toBeTruthy();
  });
});
