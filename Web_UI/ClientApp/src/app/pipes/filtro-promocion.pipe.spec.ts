import { FiltroPromocionPipe } from './filtro-promocion.pipe';

describe('FiltroPromocionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPromocionPipe();
    expect(pipe).toBeTruthy();
  });
});
